<%= form_with(model: destination_post) do |form| %>
  <% if destination_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(destination_post.errors.count, "error") %> prohibited this destination_post from being saved:</h2>

      <ul>
        <% destination_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
